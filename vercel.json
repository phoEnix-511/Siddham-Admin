{
  "buildCommand": "sh -lc 'if [ \"$VERCEL_ENV\" = \"production\" ]; then npx prisma generate && npx prisma migrate deploy; else npx prisma generate && npx prisma db push; fi && npm run build'",
  "installCommand": "npm install",
  "framework": "nextjs"
}
